# Draft PR — feature/strategia-a-medici-recovery → main

> Documento di riferimento per aprire la PR manualmente su GitHub.
> URL diretto per aprire la PR:
> https://github.com/Peppemira/booking-engine/compare/main...feature/strategia-a-medici-recovery

---

## Title (≤70 char)

```
Strategia A medici + sync candidato portale + P1 link delivery
```
(63 char)

## Body markdown (incolla nel campo description di GitHub)

```markdown
## Summary

Branch con 24 commit (vs main) di feature trasversali su sync portale,
calcolo scadenze e self-service candidato.

### 1. Strategia A — recovery storico rinnovi medici/CQC (commit pre-esistenti)
- Pipeline per-persona dal Portale dell'Automobilista bypassando finestra
  31gg della ricerca standard. Risultato GM: 4458 rinnovi medici + 1394 CF
  recuperati, copertura scadenze medico 100% (4480/4480).
- `scadenzeService` con regole art. 126 CdS (10/5/3/2 anni per età × cat.
  leggera/pesante, gestione 29-feb). 43 unit test.
- Endpoint + widget + pagina `/scadenze-mediche` con 4 bucket.
- Schema `rinnovi_portale` con unique parziale + hash diff + FK candidato.

### 2. Calcolo scadenze patente GM (NEW oggi)
- Script `scripts/recovery/calcola_scadenze_patente.js` (clone del medico)
- Profila + dry-run + run reale: **2952/2952 rinnovi patente GM popolati**
  (100%, 0 errori, ~12s) usando `data_inserimento` come riferimento +
  default cat. "B" leggera (necessario perché `categoria_patente` era null
  in `rinnovi_portale`, ma 100% dei candidati linkati avevano la categoria
  in `candidates` quindi default mai usato sul dataset reale).

### 3. Sync candidato dal portale (NEW oggi)
- Backend `POST /api/portal/sync-candidate-from-portal` con dryRun + diff:
  helper `extractPortalAnagrafica` mappa campi Struts2 → colonne DB,
  produce array `{field, current, portal, willChange}`.
- Frontend `SyncCandidateBanner` con preview modale dei diff prima
  dell'apply, gestisce errori 4xx/5xx con feedback inline.

### 4. Patente sostituita auto-update (NEW oggi)
- Backend detect regex `sostituit[ao]…patente numero (XXX)` nel response
  portale, espone `detectedPatenteUpdate` in page-form-submit.
- Frontend banner amber con bottone "Aggiorna candidato" → POST
  `/api/portal/update-candidate-patente`.

### 5. WYSIWYG fix form portale (NEW oggi)
- Frontend invia ESATTAMENTE quello che è visibile (merge field.value
  pre-compilati + override utente). Aggiunti "Svuota tutti" + "Ripristina
  valori portale" + indicator amber "PRE-COMPILATO". Risolve bug
  "patente e CF non congruenti".

### 6. P1 — Link delivery automatico al candidato (NEW oggi)
Sotto-progetto del programma "Self-service candidato" (P1→P2→P3→P4).
Spec in `docs/superpowers/specs/2026-04-17-p1-link-delivery-design.md`,
plan in `docs/superpowers/plans/2026-04-17-p1-link-delivery.md`.

- **Backend**: nuova migration `sql/2026-04-17_remote_capture_deliveries.sql`
  (RLS multi-tenant, FK CASCADE su `remote_capture_sessions`); nuovo template
  `remote_capture_link` in `notificheService` (HTML + WhatsApp + helper);
  nuovo endpoint `POST /api/remote-capture/sessions/:token/deliver`
  in modulo dedicato (`server/remoteCaptureDeliverHandler.js`).
- **Frontend**: nuovo componente `lib/SendLinkPopover.js` (popover ancorato
  con generazione token TTL 24h, canale Email via Brevo, canale WhatsApp
  via wa.me, copia link); helper `lib/phoneUtils.js` (`cleanPhone`,
  `isValidEmail`); pulsante "📲 Invia link" integrato nella sidebar
  destra dell'editor candidato.
- **Test**: 15 test nuovi (`tests/notificheServiceTemplate.test.js` +
  `tests/remoteCaptureDeliver.test.js`), tutti verdi. Suite completa:
  88/88 backend test passano.

### 7. Bonus
- **UX audit menu sidebar** (`docs/ux-audit-menu-2026-04-17.md`): identifica
  31 voci → 9 proposte, 8 pagine orfane, 10 cluster di duplicazione,
  5 quick wins + 9 migrazioni da fare con spec separate.
- **Documentazione env cron archivio**: aggiunte 5 var mancanti in
  `.env.example` + `.env.railway.example` (ARCHIVIO_SCHEDULER_ENABLED,
  ecc.). Lo scheduler era silenziosamente disabilitato in produzione.
- **Fix TDZ pre-esistente** in `app/candidati/page.js`: spostato
  `selectedRow` useMemo prima dei `useCallback` che lo usano.
- **Fix typo URL** `/api/portale` → `/api/portal` in `strumenti-utili`.
- **Cleanup `.gitignore`**: aggiunte eccezioni per `.env.example` (erano
  bloccati erroneamente dal pattern `.env.*`); tracking script debug
  temporanei e `.claude/settings.local.json` rimosso.

## Test plan

- [ ] `cd gestionale/backend && npm test` — 88/88 test passano
- [ ] Applicare migration in Supabase Studio → SQL Editor:
      `gestionale/backend/sql/2026-04-17_remote_capture_deliveries.sql`
- [ ] Smoke test P1 manual: login `/candidati` → seleziona candidato →
      click "📲 Invia link" → verifica popover + invia email + apri WA
- [ ] Verifica scadenze patente GM:
      `SELECT count(*) FROM rinnovi_portale WHERE autoscuola_id='9380513a-99ad-4067-adc7-493af2e083d1' AND tipo_rinnovo='patente' AND data_scadenza IS NOT NULL`
      → atteso 2952
- [ ] (Opzionale) Settare `ARCHIVIO_SCHEDULER_ENABLED=true` su Railway
      quando vai in produzione

## Note operative

- **Migration SQL da applicare**: `2026-04-17_remote_capture_deliveries.sql`
  (idempotente con `IF NOT EXISTS`)
- **Variabili d'ambiente nuove**: `ARCHIVIO_*` (5 var, ottimizzate per Railway)
- **No conflitti rilevati con main**: fast-forward possibile

## Diff stats

- 24 commit ahead di main
- ~3200 LOC nette aggiunte (di cui ~1500 P1)
- 60+ file toccati (ma molti sono refactor anagrafica-iscrizioni del lavoro pregresso)
- 88 test verdi
```

---

## Come aprire la PR (manuale, gh CLI non disponibile)

1. Vai su: https://github.com/Peppemira/booking-engine/compare/main...feature/strategia-a-medici-recovery
2. Click "Create pull request"
3. Title: copia/incolla il title qui sopra
4. Description: copia/incolla il body markdown qui sopra (è il blocco `</code><code>markdown ... </code><code>` SENZA i fence di markdown stessi)
5. Click "Create pull request"
6. (Opzionale) Aggiungi label "feature", reviewer, milestone
